<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature android:name="android.hardware.camera" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature android:name="android.hardware.camera.autofocus" />
    <uses-feature android:name="android.hardware.camera.flash" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />




    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SugarSync"
        android:enableOnBackInvokedCallback="true"

        tools:targetApi="31">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".Login_Activity"
            android:exported="true">

        </activity>

        <activity
            android:name=".Question_Activity"
            android:exported="true">
            <!-- Add any intent filters or other attributes specific to Register_Activity here -->
        </activity>

        <activity
                android:name=".Home_Activity"
            android:exported="true">
            <!-- Add any intent filters or other attributes specific to Register_Activity here -->
        </activity>

        <activity
            android:name=".Glucose_Activity"
            android:exported="true">
            <!-- Add any intent filters or other attributes specific to Register_Activity here -->
        </activity>

        <activity
            android:name=".Diet_Activity"
            android:exported="true">
            <!-- Add any intent filters or other attributes specific to Register_Activity here -->
        </activity>

        <activity
            android:name=".Exercise_Activity"
            android:exported="true">
            <!-- Add any intent filters or other attributes specific to Register_Activity here -->
        </activity>

        <activity
            android:name=".Profile_Activity"
            android:exported="true">
            <!-- Add any intent filters or other attributes specific to Register_Activity here -->
        </activity>

        <activity
            android:name=".ViewAllDietsActivity"
            android:exported="true">
            <!-- Add any intent filters or other attributes specific to Register_Activity here -->
        </activity>

        <activity
            android:name=".ViewAllExerciseActivity"
            android:exported="true">
            <!-- Add any intent filters or other attributes specific to Register_Activity here -->
        </activity>
    </application>

</manifest>